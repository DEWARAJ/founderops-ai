services:
  founderops:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - founderops-data:/app/data
    environment:
      FOUNDEROPS_PROVIDER: deterministic
      RETELL_WEBHOOK_API_KEY: ${RETELL_WEBHOOK_API_KEY:-}

volumes:
  founderops-data:
