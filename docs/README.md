# Project images

The PNG files in this folder are captured from the running local application using synthetic candidate data:

- `dashboard.png` — completed human-reviewed workflow
- `candidate-intake.png` — privacy-aware resume intake modal
